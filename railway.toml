[build]
builder = "dockerfile"
# Railway builds from repo root - Dockerfiles handle monorepo structure

# For frontend service, set in Railway dashboard:
# - Dockerfile Path: apps/frontend/Dockerfile

# For backend service, set in Railway dashboard:
# - Dockerfile Path: apps/backend/Dockerfile
